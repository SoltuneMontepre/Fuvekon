'use client'

import Loading from '@/components/common/Loading'
import React from 'react'

const GlobalLoading = (): React.ReactElement | null => {
	return <Loading />
}

export default GlobalLoading
