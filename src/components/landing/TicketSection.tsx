import React from 'react'

interface TicketSectionProps {
	id: string
}

const TicketSection = ({ id }: TicketSectionProps) => {
	return (
		<div id={id} className='h-screen w-dvw section'>
			handleLogout
		</div>
	)
}

export default TicketSection
